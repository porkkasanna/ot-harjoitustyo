[project]
name = "poetry-testi"
version = "0.1.0"
description = ""
authors = [
    {name = "Sanna Porkka",email = "sanna.porkka@helsinki.fi"}
]
requires-python = "^3.12"
dependencies = [
    "cowsay (>=6.1,<7.0)"
]
[tool.poetry]
package-mode = false

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "pytest (>=9.0.2,<10.0.0)"
]
